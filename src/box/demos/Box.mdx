import { Subtitle, ArgsTable } from '@storybook/addon-docs/blocks';
import { Meta, Story, Canvas } from '@storybook/addon-docs';
import Box from '../Box';
import { BasicBox } from './Box.stories';

<Meta title="MDX/Box" component={Box} />

# Box Chart 箱型图

## 基础样式

- todo

## 参数说明

<ArgsTable of={Box} />

export const Template = (args) => <Box {...args} />;

<Canvas>
  <Story id="charts-箱型图-box--box-basic" name={BasicBox.storyName} args={BasicBox.args}>
    {Template.bind({})}
  </Story>
</Canvas>

