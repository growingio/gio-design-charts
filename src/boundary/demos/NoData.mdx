import { Subtitle, ArgsTable } from '@storybook/addon-docs/blocks';
import { Meta, Story, Canvas } from '@storybook/addon-docs';
import Area from '../../area/Area';

<Meta title="MDX/NoData" component={Area} />

export const Template = (args) => <Area {...args} />;

默认的`No Data`页面

<Canvas>
  <Story id="components-no-data--no-data">{Template.bind({})}</Story>
</Canvas>

通过 `noData` props 生成`No Data`页面

<Canvas>
  <Story id="components-no-data--no-data-custom">{Template.bind({})}</Story>
</Canvas>
