import { Subtitle, ArgsTable } from '@storybook/addon-docs/blocks';
import { Meta, Story, Canvas } from '@storybook/addon-docs';
import Area from '../../area/Area';

<Meta title="MDX/ErrorBoundary" component={Area} />

export const Template = (args) => <Area {...args} />;

默认的`Error Boundary`页面

<Canvas>
  <Story id="components-errorboundary--error-boundary">{Template.bind({})}</Story>
</Canvas>

通过 `errorTemplate` props 生成 `Error Boundary` 页面

<Canvas>
  <Story id="components-errorboundary--error-boundary-custom">{Template.bind({})}</Story>
</Canvas>
