import { Subtitle, ArgsTable } from '@storybook/addon-docs/blocks';
import { Meta, Story, Canvas } from '@storybook/addon-docs';
import Area from '../../../area/Area';

<Meta title="MDX/Loading" component={Area} />

export const Template = (args) => <Area {...args} />;

默认的`Error Boundary`页面

<Canvas>
  <Story id="components-errorboundary--error-boundary">{Template.bind({})}</Story>
</Canvas>
